<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <!-- ユーザー名の存在確認 -->
    <select id="existsByUsername" resultType="int">
        SELECT
            COUNT(id)
        FROM
            mst_user
        WHERE
            username = #{username}
    </select>

    <!--    &lt;!&ndash; Discordユーザー名の存在確認 &ndash;&gt;-->
    <!--    <select id="existsByUsernameDiscord" resultType="int">-->
    <!--        SELECT-->
    <!--            COUNT(id)-->
    <!--        FROM-->
    <!--            mst_user-->
    <!--        WHERE-->
    <!--            discord_name = #{username}-->
    <!--    </select>-->

    <!--    &lt;!&ndash; ユーザー情報の取得 &ndash;&gt;-->
    <!--    <select id="existsByUsernameAndPasswordAndId" resultType="com.example.demo.entity.MasterUser">-->
    <!--        SELECT-->
    <!--            *-->
    <!--        FROM-->
    <!--            mst_user-->
    <!--        WHERE-->
    <!--            username = #{username}-->
    <!--    </select>-->

    <!--    &lt;!&ndash; ユーザーの挿入 &ndash;&gt;-->
    <!--    <insert id="insert">-->
    <!--        INSERT INTO-->
    <!--            mst_user (username, password)-->
    <!--        VALUES-->
    <!--            (#{username}, #{password})-->
    <!--    </insert>-->

</mapper>